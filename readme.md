# 🆔 NexID System

**NexID** คือระบบจัดการสมาชิก (Membership System) ที่ทันสมัย พัฒนาด้วยสถาปัตยกรรมแบบ **API-First** โดยแยกส่วน Frontend และ Backend ออกจากกันอย่างชัดเจน รองรับการขยายระบบในอนาคต (Scalable) และมีความปลอดภัยสูงด้วย **JWT Authentication**

![Project Status](https://img.shields.io/badge/status-active-success.svg)
![PHP Version](https://img.shields.io/badge/php-8.1-777BB4.svg)
![Docker](https://img.shields.io/badge/docker-ready-2496ED.svg)

## ✨ ฟีเจอร์หลัก (Key Features)

* **Authentication:** ระบบสมัครสมาชิก (Register) และเข้าสู่ระบบ (Login) ด้วย JSON Web Token (JWT)
* **Security:** เก็บหัสผ่านแบบ Hash (Bcrypt) และป้องกัน SQL Injection ด้วย PDO Prepare Statement
* **Profile Management:** ดูและแก้ไขข้อมูลส่วนตัว (ชื่อ, ที่อยู่, เบอร์โทร)
* **Modern UI:** หน้าจอออกแบบด้วย Bootstrap 5 สวยงาม รองรับมือถือ (Responsive)
* **Dashboard:** หน้าภาพรวมระบบพร้อมกราฟสถิติ (Chart.js)
* **Auto Database Init:** สร้างตารางฐานข้อมูลให้อัตโนมัติเมื่อรันครั้งแรก

## 🛠 Tech Stack

**Backend:**
* PHP 8.1 (Native API)
* MySQL 5.7
* Firebase PHP-JWT (Library)
* PHP Dotenv (จัดการ Environment Variables)

**Frontend:**
* HTML5 / CSS3
* Bootstrap 5 (UI Framework)
* Axios (HTTP Client)
* SweetAlert2 (Alert Popups)
* Chart.js (Data Visualization)

**DevOps:**
* Docker & Docker Compose

---

## 📂 โครงสร้างโปรเจกต์ (Project Structure)

```text
/nexid-project
│
├── docker-compose.yml      # Config หลักสำหรับจำลอง Server
├── Dockerfile              # Config สำหรับ PHP Image
├── .env                    # ไฟล์เก็บค่า Config และรหัสผ่าน (ห้ามเอาขึ้น Git)
├── README.md               # คู่มือการใช้งาน
│
├── /db
│   └── init.sql            # Script สร้างตารางอัตโนมัติเมื่อเริ่มระบบ
│
└── /src                    # Source Code ทั้งหมด
    ├── api/                # Backend PHP API
    │   ├── config.php
    │   ├── login.php
    │   ├── register.php
    │   └── ...
    ├── vendor/             # Composer Libraries
    ├── index.html          # หน้า Login
    ├── register.html       # หน้าสมัครสมาชิก
    ├── dashboard.html      # หน้า Dashboard
    └── profile.html        # หน้าแก้ไขข้อมูล

```   
🚀 วิธีติดตั้งและเริ่มใช้งาน (Installation)

1. Clone โปรเจกต์
```bash
git clone [https://github.com/e29ckg/NexID-System.git](https://github.com/e29ckg/NexID-System.git)
cd nexid
```


2. ตั้งค่า Environmentก๊อปปี้ไฟล์ .env.example เป็น .env (หรือสร้างใหม่ตามตัวอย่างด้านล่าง)Bash# ตัวอย่างไฟล์ .env
```text
APP_NAME="NexID System"
BASE_URL=http://localhost:8080

# Database
DB_CONNECTION=mysql
DB_HOST=db
DB_PORT=3306
DB_DATABASE=nexid_db
DB_USERNAME=user
DB_PASSWORD=password
DB_ROOT_PASSWORD=rootpassword

# JWT Security
JWT_SECRET=ใส่รหัสลับที่ยาวและเดายากๆตรงนี้
JWT_ALGO=HS256
JWT_EXPIRATION=3600
```

3. เริ่มต้นระบบ (Run with Docker)
สั่งรัน Server และ Database:

```Bash

docker-compose up -d --build
```
Note: รอสักครู่เพื่อให้ Database เริ่มทำงาน ระบบจะสร้างตารางต่างๆ ให้อัตโนมัติ

ติดตั้ง PHP Library (Composer):

```Bash

docker-compose exec web composer install
```
ขั้นตอนนี้จำเป็นมาก เพื่อโหลดไลบรารีต่างๆ เช่น JWT, DotEnv

4. เข้าใช้งาน
* หน้าเว็บไซต์: http://localhost:8080
* จัดการฐานข้อมูล (phpMyAdmin): http://localhost:8081

## 🧪 การทดสอบ (Testing)
คุณสามารถทดสอบระบบได้โดย:เข้าหน้าเว็บกด "สมัครสมาชิก"นำ Username/Password ที่สมัครมา "เข้าสู่ระบบ"ระบบจะพาไปที่หน้า Dashboard และสามารถแก้ไขข้อมูลส่วนตัวได้
## 📝 API Documentation (เบื้องต้น)

| Method | Endpoint | Description | Auth Required |
| :--- | :--- | :--- | :---: |
| `POST` | `/api/register.php` | สมัครสมาชิกใหม่ | ❌ |
| `POST` | `/api/login.php` | เข้าสู่ระบบ (รับ Token) | ❌ |
| `GET` | `/api/get_profile.php` | ดึงข้อมูลส่วนตัว | ✅ |
| `POST` | `/api/update_profile.php` | อัปเดตข้อมูลส่วนตัว | ✅ |

> **Note:** Endpoints ที่ต้องการ Auth ต้องแนบ Header `Authorization: Bearer <token>`

---

## 🤝 การแก้ไขปัญหาเบื้องต้น (Troubleshooting)

**Q: เข้า localhost:8080 ไม่ได้**
* **A:** ตรวจสอบว่ามีโปรแกรมอื่นแย่งใช้ Port 8080 อยู่หรือไม่ (เช่น Tomcat, Jenkins, XAMPP) ถ้ามีให้แก้ Port ในไฟล์ `docker-compose.yml` (เช่นเปลี่ยนเป็น `8888:80`)

**Q: Database Error / Connection Failed**
* **A:** ตรวจสอบไฟล์ `.env` ว่าค่า `DB_HOST=db` ถูกต้องหรือไม่ (ต้องไม่ใช่ localhost) และลองสั่ง `docker-compose down -v` แล้ว up ใหม่เพื่อ Reset Database

---

### Author
Developed by **E29CKG**